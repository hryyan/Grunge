#include <cstdio>
#include <iostream>
using namespace std;

const int MAX_N = 12000;
const int MAX_K = 120;

struct Custom
{
    int h, m, s;
    int t;
    int cost;
};

Custom

int main()
{
    freopen("PAT1017.input", "r", stdin);
    
    int n, k, h, m, s, cost;
    cin >> n >> k;
    for (int i = 0; i < n; i++)
    {
        scanf("%d:%d:%d %d", &h, &m, &s, &cost);

    }
}